{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main mobile window. Includes deep-link + store + notification, used on every target (desktop + mobile) for URL scheme handling, WalletConnect v2 session persistence, and native OS notifications for confirmed/failed txs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "deep-link:default",
    "store:default",
    "notification:default"
  ]
}
