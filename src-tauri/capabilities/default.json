{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main mobile window. Includes deep-link + store, used on every target (desktop + mobile) for URL scheme handling and WalletConnect v2 session persistence.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "deep-link:default",
    "store:default"
  ]
}
