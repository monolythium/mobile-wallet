{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the main mobile window. Stage 0 keeps this minimal.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
